(()=>{const modules=[...document.querySelectorAll('[data-comparison]')];if(!modules.length)return;const header=()=>document.querySelector('.site-header')?.getBoundingClientRect().height||76;function update(){const h=header();modules.forEach(m=>{const bar=m.querySelector('[data-sticky-prices]'),scroll=m.querySelector('[data-comparison-scroll]');if(!bar||!scroll)return;const mr=m.getBoundingClientRect(),sr=scroll.getBoundingClientRect();const should=sr.top<h+8&&mr.bottom>h+bar.offsetHeight+24;if(should){const c=m.closest('.container')?.getBoundingClientRect()||m.getBoundingClientRect();bar.classList.add('is-active');bar.style.setProperty('--sticky-left',`${Math.max(14,c.left)}px`);bar.style.setProperty('--sticky-width',`${Math.min(c.width,innerWidth-28)}px`);bar.style.setProperty('--sticky-top',`${h}px`)}else bar.classList.remove('is-active')})}addEventListener('scroll',update,{passive:true});addEventListener('resize',update,{passive:true});update();})();